{
  "config": {
    "abort": {
      "already_configured": "This API endpoint and model combination is already configured."
    },
    "step": {
      "user": {
        "description": "Enter the endpoint, API key and model ID. The integration will try to detect the API protocol automatically.\n\n{error_detail}",
        "data": {
          "api_key": "[%key:common::config_flow::data::api_key%]",
          "api_base": "API Base URL",
          "chat_model": "Model ID",
          "max_tokens": "Max output tokens",
          "api_mode": "API mode"
        },
        "data_description": {
          "api_base": "Example: {example_url}",
          "chat_model": "Examples: gpt-4o-mini, claude-sonnet-4-6, gemini-2.5-flash",
          "max_tokens": "Maximum output token budget for validation and conversation responses.",
          "api_mode": "Auto detect is recommended. Manually select only if the endpoint is non-standard."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "unknown": "[%key:common::config_flow::error::unknown%]",
      "unsupported_api_mode": "Unsupported API mode"
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "prompt": "Instructions",
          "chat_model": "[%key:common::generic::model%]",
          "max_tokens": "Maximum tokens to return in response",
          "temperature": "Temperature",
          "top_p": "Top P",
          "llm_hass_api": "[%key:common::config_flow::data::llm_hass_api%]",
          "recommended": "Recommended model settings"
        },
        "data_description": {
          "prompt": "Instruct how the LLM should respond. This can be a template."
        }
      }
    }
  },
  "exceptions": {
    "invalid_config_entry": {
      "message": "Invalid config entry provided. Got {config_entry}"
    }
  }
}